import { styled } from '@/styles'

export const PageContainerElement = styled('div', {
  position: 'absolute',
  top: 0,
  bottom: 0,
  left: '19rem',
  right: 0,
  width: 'calc(100vw - 19rem)',
  zIndex: -1,
})
